---
interface Props {
    date : Date
}

const { date } = Astro.props;
---

<time datetime={date.toISOString()} class="date">
    {
        date.toLocaleDateString("en-us", {
            day: 'numeric',
            month: 'short',
            year: 'numeric'
        })
    }
</time>